language: node_js
stages:
  - name: test
  - name: deploy
    if: tag IS present

node_js: 
  - "lts/*"
install:
  - npm i -g codecov
  - npm i
script:
  - npm run lint
  - npm test
  - codecov
jobs:
  include:
    - stage: deploy
      script: ./scripts/release.sh